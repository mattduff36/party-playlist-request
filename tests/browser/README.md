# Optional browser helpers

These scripts are **not** part of `finalise:full`. They are legacy/manual HTTP smoke helpers.

Prefer:

- `npm run test:api` for HTTP coverage
- `npm run test:e2e` for Playwright UI coverage
